import React from "react";

const HeaderApp: React.FC = () => {
  return (
    <header className="header-app">
      {/* App header content */}
      <div>{/* Logo, user menu, etc. */}</div>
    </header>
  );
};

export default HeaderApp;
